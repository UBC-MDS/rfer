get_ci <- function(x,interval,point_estimate){
  #Get_ci calculates the confidence intervals from the resampled groups set to an interval between 0 and 1
  
  #Parameters
  
  # x: tibble 
  #Tibble containing the summary statistics for each sample
  # interval: float
  #An integer between 0 and 1 that sets the level of confidence
  # point_estimate: float
  
  #Outputs
  
  #Tibble with a row and columns for the lower/upper bounds of the confidence interval, the point estimate, and confidence level
  
}