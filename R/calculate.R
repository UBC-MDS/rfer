#' Title
#'
#' @param x
#' @param stat
#'
#' @return
#' @export
#'
#' @examples
calculate <- function(x,stat){

  #Calculate function takes a tibble containing the resampled groups and groups them and calculates a summary statistic for each group
  #For this milestone, we've chosen to go with the mean as the statistic

  #Parameters

  # x: tibble
  #Tibble containing all resampled observations
  # stat: string
  #Specifies what type of summary statistic is to be calculated. For now, only mean is available.

  #Outputs

  #Tibble containing the summarized data, one for each resampled group (n_samples specified in generate)

}
